## 1. README

- [x] 1.1 Create `README.md` at the repository root describing the project as a simple Task Management REST API built to learn Spec Driven Development and OpenSpec, and verify the file renders correctly on GitHub/locally (headings, code blocks).
- [x] 1.2 Document the tech stack, project layout (Domain/Application/Infrastructure/Api), and how to build/run the API and tests, and verify the documented commands (`dotnet build`, `dotnet run`, `dotnet test`) succeed against the current solution.
- [x] 1.3 Add a section pointing to the `openspec/` directory (specs and changes) explaining how it demonstrates the SDD/OpenSpec workflow, and verify the referenced paths (`openspec/specs`, `openspec/changes`) exist in the repo.
