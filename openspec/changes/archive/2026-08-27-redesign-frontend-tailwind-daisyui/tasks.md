## 1. Tailwind + DaisyUI setup

- [x] 1.1 Add `tailwindcss`, `@tailwindcss/postcss`, `daisyui`, and (if the Angular builder doesn't already resolve it transitively) `postcss` as dev dependencies in `web/package.json`, and verify `npm install` succeeds with no peer-dependency errors
- [x] 1.2 Register `@tailwindcss/postcss` via a `.postcssrc.json` at `web/`, and replace `web/src/styles.css` with `@import "tailwindcss";` + `@plugin "daisyui";`; verify `npm start` boots without PostCSS/build errors and Tailwind's preflight reset is visibly applied to `web/src/index.html`
- [x] 1.3 Define the `gerenciador-dark` DaisyUI theme in `styles.css` (`@plugin "daisyui/theme"`) using the palette from `prototipo/_ds/nocturne-*/styles.css` (`base-100` #161826, `base-200` #232532, `base-content` #e9e9ed, `primary` #9184d9, plus neutral/accent ramps and `radius-md`/`radius-sm`), and verify a plain DaisyUI `btn btn-primary` rendered on any page picks up the theme's colors

## 2. Login screen

- [x] 2.1 Rewrite `web/src/app/login/login.html` using Tailwind/DaisyUI classes (`card`, `input`, `btn`) following `prototipo/export-src/Login.html`'s structure, keeping the existing `[formGroup]`/`formControlName` bindings and applying DaisyUI's `input-error` class (alongside the existing helper-text markup) to a field when its control is invalid and touched; verify the login form renders styled and centered at `http://localhost:4200/login`, and that submitting an empty field visibly shows the `input-error` state
- [x] 2.2 Remove the `MatButtonModule`/`MatCardModule`/`MatFormFieldModule`/`MatInputModule`/`MatProgressSpinnerModule` imports from `web/src/app/login/login.ts`, replace the Material spinner with a DaisyUI `loading loading-spinner` element, and delete `web/src/app/login/login.css` content that only existed to offset Material defaults; verify `npm test` (full suite) still passes — there is no dedicated `login.*.spec.ts` today, so this only confirms nothing elsewhere broke; the login flow itself is verified manually in 2.3
- [x] 2.3 Verify by hand: submitting valid credentials navigates to `/tasks`, and submitting invalid credentials shows the inline error message and clears the password field, matching `openspec/specs/task-management-ui/spec.md` (Successful Login / Invalid Login Credentials) — verified against the running app (API + `ng serve`) via a headless-browser pass: valid login navigates to `/tasks`; invalid login shows "Email ou senha inválidos." and clears the password field

## 3. Cadastro (register) screen

- [x] 3.1 Rewrite `web/src/app/register/register.html` following `prototipo/export-src/Cadastro.html`'s structure (same card/input/button classes as Login), keeping existing form bindings, applying DaisyUI's `input-error` class to invalid/touched fields, and the password-requirements validator error text; verify the form renders styled at `http://localhost:4200/register`, and that an invalid field visibly shows the `input-error` state
- [x] 3.2 Remove the Material module imports from `web/src/app/register/register.ts` and its Material-only CSS, mirroring task 2.2; verify `npm test` (full suite) still passes — neither `register.*.spec.ts` nor `core/password-requirements.validator.spec.ts` exists today, so there is no dedicated automated coverage for this screen; the registration flow itself is verified manually in 3.3
- [x] 3.3 Verify by hand: registering a new account logs the user in and lands on `/tasks`; registering a duplicate email shows the inline "already in use" error, matching the spec's Registration requirement — verified against the running app: registration auto-logs in and lands on `/tasks`; re-registering the same email shows "O email '...' já está em uso." and stays on `/register`

## 4. Tarefas (task list) screen

- [x] 4.1 Rewrite `web/src/app/tasks/task-list/task-list.html` as a responsive grid of DaisyUI `card`s (`grid-cols-[repeat(auto-fill,minmax(280px,1fr))]`) following `prototipo/export-src/Tarefas.html`, including the DaisyUI `skeleton` loading state, the error state with a "Tentar novamente" button, and the empty state; verify each state with a concrete, reproducible trigger: loading is visible during the normal initial fetch (observe on page load/reload); error state by stopping the API process and reloading `/tasks`; empty state by deleting all of a test account's tasks (via the UI or `DELETE /tasks/{id}`) and reloading; ready state is the normal case with the API running and tasks present — verified against the running app: skeleton visible during a delayed fetch, error state + "Tentar novamente" shown with the API stopped, empty state shown for a fresh/emptied account, and the ready-state grid renders correctly
- [x] 4.2 Replace the top `mat-toolbar` with a Tailwind/DaisyUI `nav` bar (brand + "Sair" logout button) matching the prototype's `.nav`, and replace each task's `mat-select` status control with a DaisyUI `select select-sm`; verify status changes still call `TasksService.update(...)` and persist (reload the page and confirm the new status sticks); verify logout still ends the session and shows the login screen, and that using the browser's back button afterward does not reveal task data (matches spec's Logout Ends the Session / Browser back navigation after logout scenarios) — verified against the running app: logout ends the session and returns to `/login`, and back-navigation afterward does not show task data. Manual verification also caught a real bug: the status `<select>` always displayed "Pendente" regardless of the task's actual status (badge was correct, only the dropdown was wrong), because `[value]` was bound on the `<select>` itself and evaluated before its `@for`-rendered `<option>`s existed. Fixed by moving the selection onto each `<option>` via `[selected]="option.value === task.status"` instead; re-verified the select now shows the correct option immediately and after reload, and `npm test` still passes (16/16)
- [x] 4.3 Create a new standalone `ConfirmDeleteDialog` component backed by a native `<dialog>` element (`showModal()`/`close()`) styled with DaisyUI `.modal`/`.modal-box`, replacing the `MatDialog`-based one at `web/src/app/tasks/confirm-delete-dialog/`; update `task-list.ts` to open/close it without `MatDialog`/`MAT_DIALOG_DATA`; verify deleting a task still shows the confirmation dialog, cancelling keeps the task, and confirming removes it (matches spec's Task Deletion Requires Confirmation scenarios) — verified against the running app: delete click opens the dialog, "Cancelar" keeps the task, "Excluir" removes it and returns to the empty state
- [x] 4.4 Remove all `Mat*Module` imports from `web/src/app/tasks/task-list/task-list.ts`; verify `npx ng test --watch=false tasks.service.spec.ts` still passes (the only existing automated coverage in this area) and `npm test` (full suite) still passes overall — there is no dedicated `task-list.*.spec.ts` today, so the list/grid/dialog behavior itself is verified manually in 4.1–4.3

## 5. Formulário de tarefa (task form) screen

- [x] 5.1 Rewrite `web/src/app/tasks/task-form/task-form.html` following `prototipo/export-src/FormularioTarefa.html`: DaisyUI `input`/`textarea` for title/description, and a segmented `join` of radio-styled buttons (matching the prototype's `.seg`) replacing the `mat-select` status field; keep the existing `[formGroup]` bindings, apply DaisyUI's `input-error` class to invalid/touched fields, and keep the validation error text; verify creating and editing a task both render the correct heading ("Nova tarefa" / "Editar tarefa") and pre-fill correctly in edit mode, and that an invalid field visibly shows the `input-error` state — verified against the running app: new-task heading is "Nova tarefa", edit heading is "Editar tarefa" with the title pre-filled, and clearing+touching the title shows the `input-error` class
- [x] 5.2 Remove the Material module imports and Material-only CSS from `web/src/app/tasks/task-form/task-form.ts`/`.css`; verify `npm test` (full suite) still passes — there is no dedicated `task-form.*.spec.ts` today, so this only confirms nothing elsewhere broke; the form behavior itself is verified manually in 5.3
- [x] 5.3 Verify by hand: submitting an empty title keeps the user on the form with the inline error and does not save; submitting a valid edit or creation saves and returns to `/tasks` showing the change, matching the spec's Task Creation / Task Editing requirements — verified against the running app: empty title stays on the form with "Informe o título." and doesn't save; a valid creation and a valid edit both save and return to `/tasks` showing the new/changed task

## 6. Cleanup and final verification

- [x] 6.1 Remove `@angular/material` and `@angular/cdk` from `web/package.json`, delete `web/src/material-theme.scss` and its entry in `web/angular.json`'s `"styles"` array, and run `npm install`; verify `grep -rn "@angular/material\|@angular/cdk\|mat-" web/src/app` returns no matches
- [x] 6.2 Run `npm run build` and verify the production build completes without errors or missing-style warnings
- [x] 6.3 Run `npm test` (full suite) and verify all frontend tests pass
- [x] 6.4 Update `CLAUDE.md`'s frontend architecture/stack description to reflect Tailwind CSS + DaisyUI in place of Angular Material, and verify the file no longer mentions `@angular/material`
